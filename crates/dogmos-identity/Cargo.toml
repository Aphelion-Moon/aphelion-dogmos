[package]
name = "dogmos-identity"
version = "2.3.0"
edition = "2021"

[target.'cfg(windows)'.dependencies]
windows-sys = { workspace = true }

[target.'cfg(not(windows))'.dependencies]
sha2 = "=0.10.9"
