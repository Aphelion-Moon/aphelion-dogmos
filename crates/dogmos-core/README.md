# Dogmos numerical kernels

Pure diffusion and heat-conduction routines used by the root engine. State and scheduling belong to the in-process engine; these kernels do not depend on BYOND. Numerical properties are exercised in `tests/numerical_properties.rs`.
